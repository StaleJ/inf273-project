package operators.greedy

import classes.World
import operators.Operator

class InsertBest: Operator<Int>() {
    override fun operator(currentSolution: MutableList<Int>, world: World): MutableList<Int> {
        TODO("Not yet implemented")
    }
}